<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bluetoothchat"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />
    
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.bluetoothchat.DiscoveryActivity"
            android:icon="@drawable/ic_launcher"
            android:label="@string/discover_activity" >
        </activity>
        <activity
            android:name="com.example.bluetoothchat.ServerAndConnectionActivity"
            android:icon="@drawable/ic_launcher_server"
            android:label="@string/bluetooth_server" >
        </activity>
        <activity
            android:name="com.example.bluetoothchat.ClientConnectionActivity"
            android:icon="@drawable/ic_launcher"
            android:label="@string/bluetooth_client" >
        </activity>
        
    </application>

</manifest>
